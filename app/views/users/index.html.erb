<h1>Users List</h1>
<p>List of users.</p>

<% @all_users.each do |user| %>
    <%= link_to user_path(user.id) do %>
        <%= render partial: "shared/user_list", locals: {user: user} %>
    <% end %>
<% end %>